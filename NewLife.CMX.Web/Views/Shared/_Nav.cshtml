@{
    var category = Model as Category;
    if (category == null)
    {
        category = (Model as Info).Category;
    }
}
<ol class="breadcrumb">
    <li><a href="~/">首页</a></li>
    @foreach (var cat in category.AllParents)
    {
        <li>
            <a href="@Url.GetCategoryUrl(cat)">@cat.Name</a>
        </li>
    }
    <li class="active">
        <a href="@Url.GetCategoryUrl(category)">@category.Name</a>
    </li>
</ol>