@model NewLife.CMX.ICategory
@{
    var cat = Model;
    var page = ViewBag.Pager as Pager;
}
<div class="container" style="min-height: 600px;">
    <div class="row">
        @Html.Partial("_Nav")
    </div>
    <h2 class="tith2">
        @cat.Name
    </h2>
    @{
        var childs = cat.Childs;
        if (childs.Count > 0)
        {
            @Html.Partial("_Category_Childs", cat)
        }
        else
        {
            <ul class="list_news_dl fixed">
                @foreach (var item in cat.GetInfos(page))
                {
                    <li>
                        <a href="@this.GetUrl(item)">
                            <i></i>@item.Title
                        </a>
                    </li>
                }
            </ul>
            @Html.Partial("_Category_Pager", cat)
        }
    }
</div>
