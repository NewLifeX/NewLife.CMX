@{
    var category = Model as ICategory;
    if (category == null)
    {
        category = (Model as IInfo).Category;
    }
}
<ol class="breadcrumb">
    <li><a href="~/">首页</a></li>
    @foreach (ICategory cat in category.AllParents)
    {
        <li>
            <a href="@this.GetCategoryUrl(cat)">@cat.Name</a>
        </li>
    }
    <li class="active">
        <a href="@this.GetCategoryUrl(category)">@category.Name</a>
    </li>
</ol>